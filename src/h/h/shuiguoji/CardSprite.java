package h.h.shuiguoji;

import android.graphics.Bitmap;
import android.graphics.Canvas;
import android.graphics.Rect;
import android.graphics.RectF;

/*
public class CardSprite
{
	Bitmap pBmp;
	RectF area;
	
	public CardSprite(Bitmap bmp,RectF rct)
	{
		pBmp = bmp;
		area = rct;
	}
	public byte CardValue(byte cardData)
	{
		return (byte)(cardData & 0xf0);
	}
	public byte CardColor(byte cardData)
	{
		return (byte)(cardData & 0x0f);
	}
	public void DrawCardBack(Canvas cvs,Rect rcCardBack)
	{
		cvs.drawBitmap(pBmp, rcCardBack, area, null);
	}
	public void DrawCardContent(Canvas cvs,byte cardData)
	{
		cvs.drawBitmap(pBmp, rcCardBack, area, null);
		cvs.drawBitmap(pBmp, rcCardBack, area, null);
	}

	//初始化牌位置
	public void InitCardPosition()
	{}
	//初始化牌数据
	public void SetCardData(byte cardData[])
	{}
	//初始化牌
	public void InitCardSprite()
	{}
	//设置原图
	public void SetArtwork()
	{}
}

*/